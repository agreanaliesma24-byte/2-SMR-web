// Selección de los elementos del DOM
const boton = document.getElementById('   ');
const textoExtra = document.getElementById('   ');

// Escuchador de eventos para el click
boton.addEventListener('click', () => {
    // Comprobamos el estado actual del párrafo
    if (textoExtra.style.display === 'block') {
        // Si es visible, lo ocultamos y cambiamos el texto
        
    } else {
        // Si está oculto, lo mostramos y cambiamos el texto
       
    }
});